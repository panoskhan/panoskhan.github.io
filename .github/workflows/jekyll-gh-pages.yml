# Disabled — site is plain HTML, static.yml handles deployment
name: Deploy Jekyll (disabled)

on:
  workflow_dispatch:

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - name: Skipped
        run: echo "Jekyll workflow disabled."
